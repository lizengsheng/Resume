# 进度

## 2026-08-19 - 批次一完成

- 从 `vito8916/simple-portfolio` 引入源码和 MIT 许可，未继承上游 Git 历史。
- 初始化独立 `main` 分支仓库。
- 将临时需求稿固化为设计、技术栈、实施计划、架构和协作约定。
- 验证：目标目录此前为空；仓库初始化成功；基础文件均处于待首次提交状态。

## 2026-08-19 - 批次二完成

- 将英文标签页模板重构为中文单页首页、两个 MDX 项目详情页和中文 404。
- 建立项目内容 Schema、base-aware 路径工具、明暗主题、响应式视觉与原创 SVG 封面。
- 从源 DOCX 提取并核对证件照，源 DOCX 未进入仓库。
- 验证：`npm run check` 为 0 错误、0 警告；`npm run build` 生成 4 个静态页面。

## 2026-08-19 - 批次三完成

- 使用确定性脚本生成一页式 `public/resume.pdf`，保留教育、项目、竞赛、技能和 GPA，删除手机号。
- 完成两轮字号、行距和内容密度调整。
- 验证：PDF 为单页；文本扫描未发现手机号或微信；最终页面渲染无裁切、重叠、缺字或孤立第二页。

## 2026-08-19 - 批次四完成

- 补齐 GitHub Pages 工作流、维护说明和线上技术博客入口；博客链接在顶部导航、首屏和联系区清晰展示。
- 依赖安全修复保持在 Astro 5 主版本内；剩余审计项需要升级到 Astro 7，未进行计划外的大版本迁移。
- 验证：最终 `astro check` 通过，生产构建生成 4 个页面；桌面、手机和深色主题无横向溢出，主题偏好可持久化，4 条核心路由均返回 200；仓库与构建产物未发现手机号、微信、源 DOCX 或绕过 `/Resume` 的内部链接。

## 2026-08-19 - 批次五完成

- 根据参考站重构为窄版居中的个人资料卡布局，恢复圆形头像、三项事实、黄色简历操作条、简洁项目网格和“作品 / 关于”标签页。
- 新增 `src/config.ts` 作为个人资料与页面文案唯一入口；网站与 PDF 默认共用可直接覆盖的 `public/profile.jpg`。
- 项目集合改为通用模型：仅标题和摘要必填，slug 可由文件名生成，时间、封面、指标、排序、精选、草稿和外链均可选；新增全部项目页。
- README 增加个人信息、头像和 Markdown 项目的三步替换指南与最小项目示例。
- 验证：`astro check` 通过；生产构建生成 5 个页面；桌面和 390px 手机页面无横向溢出或脚本错误，标签页点击和状态切换正常，全部项目页返回 200。

## 2026-08-19 - 批次六完成

- 创建公开仓库 `https://github.com/lizengsheng/Resume`，本地 `main` 已跟踪 `origin/main`。
- GitHub Pages 发布源设置为 GitHub Actions，线上地址为 `https://lizengsheng.github.io/Resume/`，HTTPS 已启用。
- 首次 Pages 工作流 `32259511135` 的 build 与 deploy 均成功。
- 线上验证：首页、全部项目页、项目详情、头像和公开 PDF 均返回 200；HTML 包含 `/Resume` 资源路径、博客链接和项目链接；浏览器中首页正常渲染，标签页交互正常且无控制台错误。

## 2026-08-19 - 批次七完成

- 网站、分享卡和未来 PDF 生成器的展示名统一改为 `Li Zengsheng`；现有 `public/resume.pdf` 按用户要求暂不替换。
- 首页公开项目数量改为从非草稿 Markdown 自动计算，补充无项目空状态和可复制的 Markdown 项目模板；页面组件、集中配置和分享卡均不再写死当前项目名称。
- 新增 `UPDATE_GUIDE.md`，覆盖项目、页面文字、链接、头像、PDF、本地检查、提交、推送、Actions、线上验证和回退流程。
- 新增递归链接检查脚本，并将线上站内链接检查加入 Pages 部署工作流；后续每次推送 `main` 都会自动构建、部署和检查。
- 验证：本地 `astro check` 与生产构建通过；本地检查 24 个链接/资源、线上检查 18 个链接/资源，内部和外部失败均为 0；唯一手动项为 `mailto:`，邮箱格式有效但是否唤起客户端取决于访问者设备。
- GitHub Actions 运行 `32260909772` 的构建、部署和部署后链接检查全部成功；线上 HTML 与分享卡已显示 `Li Zengsheng`，HTML 中不存在中文姓名。

## 2026-08-19 - 批次八完成

- 将 Astro 5 升级到 Astro 7.2.3，并将 `sharp` 更新到已修复版本；依赖审计由 3 个低危、2 个高危降为 0。
- 移除源码中未使用的 React、Radix、Tailwind、图标和工具依赖，直接依赖由 22 个收敛为 5 个；样式继续由原生 CSS 维护。
- 内容 Schema 更新为 Astro 7 / Zod 新接口；无标签项目不再生成空容器，任意数量的项目指标可自动适配列数。
- 修复手机端项目详情封面继承固定高度后产生大块留白的问题，封面现在按原始比例自适应。
- 用只包含 `title`、`summary` 和正文的临时 Markdown 完成新增项目验证，确认无需修改前端组件；验证后已删除临时项目。
- Pages 工作流增加高危依赖门禁，README 与更新指南统一说明 Node.js 22.19+、锁文件安装和依赖审计步骤。

## 2026-08-20 - 批次九完成

- 以在线技术博客为视觉基准，将浅色/深色配色、系统中文字体、768px 窄栏、81px 桌面导航、细分隔线和低圆角统一到作品集；移除旧灰底、黄色强调、强阴影与装饰图形。
- 首页由“作品 / 关于”标签页改为无需切换的连续章节，按个人资料与操作入口、精选项目、关于与技术能力、教育背景顺序完整展示；禁用 JavaScript 时核心内容仍在静态 HTML 中可读。
- 项目卡、全部项目页和详情正文统一为博客式扁平视觉；无封面、标签、指标、时间或外链时继续按内容自动省略，任意 Markdown 正文不要求固定章节。
- README、更新指南、设计文档、实施计划与架构地图已同步；头像、公开简历、项目 Schema、公开路由和 `/Resume` 基路径均未改变。
- 验证：`npm audit` 为 0 个漏洞；最小字段临时 Markdown 同时生成首页卡片、项目列表与详情页，随后删除；最终 `astro check` 为 0 错误/警告并构建 5 个页面；本地严格检查 25 个链接/资源，失败为 0，唯一手动项为 `mailto:`。
- 浏览器验收：桌面宽度为 768px、导航高度为 81px，明暗令牌与博客基准一致；390px 首页和项目详情无横向滚动，头像保持 1:1 裁切、卡片单列、指标自动单列，控制台无错误或警告。
- 首次发布发现锁文件中两个缺少版本号的可选 Rolldown 平台占位项会使全新环境的 `npm install` 报 `Invalid Version`；移除无效占位项后，`npm ci`、0 漏洞审计和完整生产构建均在干净依赖安装下通过。

## 2026-08-20 - 批次十完成

- 导航品牌精简为 `Li Zengsheng`，首页浏览器标题固定为 `Li Zengsheng's Resume`；项目详情等内页继续使用“页面名｜Li Zengsheng”。
- 首屏移除重复的项目跳转，只保留带统一下载图标的公开简历主按钮；GitHub、技术博客与邮箱使用相同尺寸、描边和颜色继承规则的轻量图标文字链接。
- 在博客同源视觉下恢复“项目 / 关于”渐进增强标签页：“项目”默认显示，支持点击、左右方向键、Home/End、`#projects`、`#about` 和刷新恢复；禁用 JavaScript 时两个面板均完整显示。
- 字体、按钮、图标与标签尺寸统一为响应式比例：桌面姓名不超过 3rem、手机姓名 2rem、功能图标 18px、主按钮约 44px、标签点击高度 48px；390px 标签等宽且项目卡保持单列。
- README、设计文档、实施计划、架构地图和更新指南已同步；项目 Schema、头像、公开 PDF、项目内容和公开路由均未修改。
- 验证：最小字段临时 Markdown 自动生成首页、项目列表和详情页后已删除；`npm audit` 为 0 个漏洞，最终 Astro 检查为 0 错误/警告并构建 5 个页面，本地严格检查 24 个链接/资源且失败为 0。
- 浏览器验收：桌面及 390px 明暗主题无横向溢出；标题、品牌、默认标签、鼠标与键盘交互、锚点刷新恢复和无脚本双面板回退均符合设计；项目详情无回归，控制台无错误或警告。

## 2026-08-20 - 批次十一完成

- 首屏岗位方向更新为“大模型后训练 · Agent · Agentic RL”，简介同步精简并在桌面保持单行、移动端自然换行；移除毕业年份、项目数量和关注方向三项事实统计及其动态计数逻辑。
- 新增可复用 `ContactLinks` 组件，首屏与页脚统一 GitHub、技术博客和 Email 的链接、图标、尺寸与顺序；页脚删除构建状态文案，桌面单行、移动端两行显示。
- “项目 / 关于”改为整行等宽分段按钮；项目面板直接显示项目卡和唯一的“查看全部项目”入口，关于面板只保留技术能力与教育背景。
- 清理不再使用的配置字段、样式选择器和维护说明；站点描述与分享卡已统一为“大模型后训练 · Agent · Agentic RL”方向，项目 Schema、头像、PDF 和公开路由未改变。
- 验证：只包含 `title`、`summary` 和正文的临时 Markdown 自动生成首页卡片、项目列表和详情页，随后删除；`npm audit` 为 0 个漏洞，最终 Astro 检查为 0 错误/警告并构建 5 个页面，本地严格检查 24 个链接/资源且失败为 0，`mailto:` 仅需访问者本机邮件客户端。
- 浏览器验收：1280px 桌面简介为单行；390px 简介为两行而联系方式保持单行；320px 联系方式安全换行；三种宽度均无横向滚动。分段按钮高 46px、明暗活动色正确，点击、方向键、锚点恢复、无脚本双面板回退和项目详情页均正常。

## 2026-08-20 - 批次十二完成

- 使用用户提供的新证件照替换 `public/profile.jpg`；原始照片保持不动，未重新生成公开简历 PDF。
- 为避免小尺寸头像加载 4.99 MB 原图，将网站副本等比例缩放为 800×1212、JPEG 质量 90，并移除无关元数据；最终文件约 126 KB，人物内容和构图未修改。
- 验证：Astro 检查为 0 错误/警告并构建 5 个页面；浏览器中 108px 圆形头像保持人物居中、无拉伸、无脸部裁切异常。
